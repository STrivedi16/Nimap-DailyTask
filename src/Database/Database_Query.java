package Database;

public class Database_Query {

	
	/*
	 * create database Query:
	 * create database name
	 * 
	 * create table table_name(fields ) ---> Create table query;
	 *
	 * insert into table_name values ( values that you want to store in  data base as per the your table)--> insert Query
	 *
	 * update  table _name set (column name = values (that is based on your requirement )) where  (column name = values (that is based on your requirement ))
	 * for example update student set name= Shubham where id=1;
	 *
	 * alter -> Used to add  or remove column from table;
	 * 
	 * truncate -> Used to delete data from the table
	 * 
	 * 
	 * 
	 */
	
}
